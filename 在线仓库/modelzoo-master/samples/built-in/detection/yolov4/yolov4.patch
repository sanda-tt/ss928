diff --git a/demo_pytorch2onnx.py b/demo_pytorch2onnx.py
index 4a7a86e..25937b1 100644
--- a/demo_pytorch2onnx.py
+++ b/demo_pytorch2onnx.py
@@ -14,13 +14,13 @@ from demo_darknet2onnx import detect
 
 def transform_to_onnx(weight_file, batch_size, n_classes, IN_IMAGE_H, IN_IMAGE_W):
     
-    model = Yolov4(n_classes=n_classes, inference=True)
+    model = Yolov4(n_classes=n_classes, inference=False)
 
-    pretrained_dict = torch.load(weight_file, map_location=torch.device('cuda'))
+    pretrained_dict = torch.load(weight_file, map_location=torch.device('cpu'))
     model.load_state_dict(pretrained_dict)
 
     input_names = ["input"]
-    output_names = ['boxes', 'confs']
+    output_names = ['feature_map_1', 'feature_map_2', 'feature_map_3']
 
     dynamic = False
     if batch_size <= 0:
@@ -73,12 +73,12 @@ def main(weight_file, image_path, batch_size, n_classes, IN_IMAGE_H, IN_IMAGE_W)
         # Transform to onnx for demo
         onnx_path_demo = transform_to_onnx(weight_file, 1, n_classes, IN_IMAGE_H, IN_IMAGE_W)
 
-    session = onnxruntime.InferenceSession(onnx_path_demo)
-    # session = onnx.load(onnx_path)
-    print("The model expects input shape: ", session.get_inputs()[0].shape)
-
-    image_src = cv2.imread(image_path)
-    detect(session, image_src)
+    # session = onnxruntime.InferenceSession(onnx_path_demo)
+    # # session = onnx.load(onnx_path)
+    # print("The model expects input shape: ", session.get_inputs()[0].shape)
+    #
+    # image_src = cv2.imread(image_path)
+    # detect(session, image_src)
 
 
 
diff --git a/models.py b/models.py
index adbee6e..a48b1b3 100644
--- a/models.py
+++ b/models.py
@@ -10,7 +10,9 @@ class Mish(torch.nn.Module):
         super().__init__()
 
     def forward(self, x):
-        x = x * (torch.tanh(torch.nn.functional.softplus(x)))
+        # ONNX模型转OM模型暂不支持算子torch.nn.functional.softplus，换成等价的最基础的数学公式方式, softplus(x) = ln(1 + exp(x))
+        softplus = torch.log(1.0 + torch.exp(x))
+        x = x * torch.tanh(softplus)
         return x
 
 
@@ -419,10 +421,10 @@ class Yolov4(nn.Module):
         self.down4 = DownSample4()
         self.down5 = DownSample5()
         # neck
-        self.neck = Neck(inference)
+        self.neek = Neck(inference)
         # yolov4conv137
         if yolov4conv137weight:
-            _model = nn.Sequential(self.down1, self.down2, self.down3, self.down4, self.down5, self.neck)
+            _model = nn.Sequential(self.down1, self.down2, self.down3, self.down4, self.down5, self.neek)
             pretrained_dict = torch.load(yolov4conv137weight)
 
             model_dict = _model.state_dict()
@@ -443,7 +445,7 @@ class Yolov4(nn.Module):
         d4 = self.down4(d3)
         d5 = self.down5(d4)
 
-        x20, x13, x6 = self.neck(d5, d4, d3)
+        x20, x13, x6 = self.neek(d5, d4, d3)
 
         output = self.head(x20, x13, x6)
         return output
