
yolov8s-world 可以通过以下代码完成快速推理, 该代码仅展示主要流程，完整实现参考src/main.cpp文件和readme
```
#include "model.h"
#include "log.h"

using namespace Infer;

int main()
{
    EnvInit();
    std::string omModelPath = "/path/to/model.om"; // yolov8s-world模型文件路径 
    std::string imagePath = "/path/to/image.jpg"; // 输入图片路径
    std::unique_ptr<Model> model = std::make_unique<Model>();
    if (model->Load(omModelPath, ModelType::Yolov8sWorld) != 0) {
        LOG(ERROR) << "fail to load model";
        return -1;
    }
    auto ret = model->Infer(imagePath, FileType::SingelImageFile);
    if (ret.size() == 0) {
        LOG(ERROR) << "fail to infer model";
        model->Unload();
        return -1;
    }
    if (model->Unload() != 0) {
        LOG(ERROR) << "fail to unload model";
        return -1;
    }
    EnvDeinit();
    return 0;
}
```
备注：头文件和动态库位于[/samples/common](https://gitee.com/HiSpark/modelzoo/tree/master/samples/common)目录下，编译配置参考[文件](https://gitee.com/HiSpark/modelzoo/blob/master/samples/samples_GPL/built-in/yolov8s-world/src/CMakeLists.txt)
