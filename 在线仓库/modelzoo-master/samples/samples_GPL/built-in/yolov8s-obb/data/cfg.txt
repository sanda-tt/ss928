# 预处理后二进制文件保存路径，为空则不保存
save_preprocess_bin="../out/preprocess/bin"

# 预处理后二进制文件列表，为空则不保存
save_preprocess_txt="../out/preprocess"

# 模型推理原始二进制结果路径（供Python精度评估用），为空则不保存
save_result_bin="../out/result/bin"

# 推理结果（类别ID等）文本文件保存路径，为空则不保存
save_result_txt="../out/result/txt"

# 模型输入图像高度（预处理resize目标值）
img_height=1024

# 模型输入图像宽度（预处理resize目标值）
img_width=1024

# 置信度阈值（过滤低置信度候选框）
conf_threshold=0.01

# NMS交并比阈值（过滤重复检测框）
nms_threshold=0.7


######## 如下参数请优先使用命令行传参 ########
# ACL配置文件路径（用于Ascend平台初始化），优先使用命令行传参
acl_config_path="../config/acl.json"

# 离线推理模型（OM文件）路径，优先使用命令行传参
om_model_path="../model/yolov8s-obb.om"

# 输入图像路径，优先使用命令行传参
img_list_path="../data/file_list.json"